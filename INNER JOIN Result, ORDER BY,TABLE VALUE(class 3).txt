1> Create EMP table:-
CREATE TABLE emp
(
  EID VARCHAR2(3) PRIMARY KEY,
  ENAME VARCHAR2(6),
  SALARY NUMBER(5),
  CITY VARCHAR2(7),
  DOB DATE          
);
INSERT INTO emp VALUES('E01','AJAY',9000,'KOLKATA','12-JAN-20');
INSERT INTO emp VALUES('E02','ROY',2000,'MUMBAI','01-JUL-10');
INSERT INTO emp VALUES('E03','NILAY',10000,'DELHI','18-FEB-12');
INSERT INTO emp VALUES('E04','BIJAY',4000,'PUNE','18-MAY-07');
INSERT INTO emp VALUES('E05','SANJAY',8000,'CHENNAI','15-MAR-15');
SELECT * FROM emp;

2> Create dep table:-
CREATE TABLE dep
(
  DID VARCHAR2(3) PRIMARY KEY,
  DNAME VARCHAR2(15)  -- Increased size for 'MARKETING' (8 chars)
);
INSERT INTO dep VALUES('D01','HR');
INSERT INTO dep VALUES('D02','IT');
INSERT INTO dep VALUES('D03','MARKETING');
SELECT * FROM dep;

3> Create WORK table:-
CREATE TABLE WORK
(
  DID VARCHAR2(3),  
  EID VARCHAR2(3), 
  PRIMARY KEY(DID,EID),
  FOREIGN KEY(DID) REFERENCES dep(DID),   
  FOREIGN KEY(EID) REFERENCES emp(EID)
);
INSERT INTO WORK VALUES('D01','E02');
INSERT INTO WORK VALUES('D03','E01');
INSERT INTO WORK VALUES('D01','E05');  
INSERT INTO WORK VALUES('D03','E02');
INSERT INTO WORK VALUES('D03','E05');  
INSERT INTO WORK VALUES('D02','E03');
INSERT INTO WORK VALUES('D02','E04');
SELECT * FROM WORK;

5>Delete the 'E04':-
i)DELETE FROM WORK WHERE EID='E04';
ii)DELETE FROM emp WHERE EID='E04';

If I can delete normally, then 
use
CREATE TABLE WORK
(
  DID VARCHAR2(3),  
  EID VARCHAR2(3), 
  PRIMARY KEY(DID,EID),
  FOREIGN KEY(DID) REFERENCES dep(DID) ON DELETE CASCADE,
  FOREIGN KEY(EID) REFERENCES emp(EID) ON DELETE CASCADE    
);

6>Find out the names of those who work in HR role:-
SELECT ENAME FROM emp, dep, WORK 
WHERE Emp.EID=WORK.EID AND Dep.DID=WORK.DID AND DNAME='HR';
Using sub query:-
SELECT ENAME FROM emp WHERE EID IN(SELECT EID FROM WORK WHERE DID IN(SELECT DID FROM Dep WHERE DNAME='HR'))

7>Find out the DNAME of those who are from MUMBAI
SELECT DNAME FROM emp, dep, WORK 
WHERE Emp.EID=WORK.EID AND Dep.DID=WORK.DID 
AND CITY='MUMBAI';

8>Alias name(temporary, alternative name)
SELECT ENAME FROM emp E, dep D, WORK W WHERE
D.DID=W.DID AND E.EID=W.EID AND DNAME='HR';

9>Assending order(order by) SALARY:-
SELECT * FROM emp ORDER BY SALARY;
10>Decending order(order by) SALARY:-
SELECT * FROM emp ORDER BY SALARY DESC;

11>In check when the table is created 
CREATE TABLE EMP1(
	EID VARCHER2(3) PRIMARY KEY,
	ENAME VARCHER2(10) CHECK(ENAME LIKE 'A%'),
	AGE NUMBER(3) CHECK(AGE>=18), 
	CITY VARCHER2(10) NOT NULL
);
INSERT INTO EMP1 VALUES('E01','Anik',45,'Kolkata');
INSERT INTO EMP1 VALUES('E02','Nil',23,'Lake Town');
--Error: CHECK constraint failed: ENAME LIKE 'A%'
INSERT INTO EMP1 VALUES('E03','Ani',10,'');
--Error: CHECK constraint failed: AGE>=18 and the city will not null
